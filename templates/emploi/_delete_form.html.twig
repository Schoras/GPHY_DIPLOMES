<form method="post" action="{{ path('emploi_delete', {'idContrat': emploi.idContrat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emploi.idContrat) }}">
    <button class="btn">Delete</button>
</form>
