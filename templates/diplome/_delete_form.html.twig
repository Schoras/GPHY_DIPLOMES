<form method="post" action="{{ path('diplome_delete', {'numEtud': diplome.numEtud}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diplome.numEtud) }}">
    <button class="btn">Delete</button>
</form>
